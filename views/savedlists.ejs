<% include partials/head.ejs %>
<header>
    <h1>To-do List</h1>
    <h2>saved lists</h2>

</header>
<section>
    <div class="savedlists">
        <% lists.forEach(function(list){ %>
        <ul>
            <h2>List</h2>
            <% list.forEach(function(items) {%>
                <li><%= items %></li>
            <% }) %>
        </ul>
        <% }) %>
    </div>
</section>

</body>
</html>